# Function to throw error if the \`phyloseq\` package is called but it is not installed

Function to throw error if the \`phyloseq\` package is called but it is
not installed

## Usage

``` r
warn_phyloseq()
```
