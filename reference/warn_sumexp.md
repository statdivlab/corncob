# Function to throw error if the \`SummarizedExperiment\` package is called but it is not installed

Function to throw error if the \`SummarizedExperiment\` package is
called but it is not installed

## Usage

``` r
warn_sumexp()
```
